import React from 'react'

import logo from '../images/logo.png'

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} alt='' width={122} height={80} />
    </div>
  )
}

export default Logo